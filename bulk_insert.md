#### 批量插入和事务
像一张表中插入若干条数据,为了保证事务的顺利进行，需要通过WAL的方式来进行保证

将记录信息, 记录操作,存储位置作为一条记录写入WAL中，进行一次批次操作之前, 先获取trx_id作为事务id,写入到WAL中，
然后每次写入记录到数据库中的时候,先写WAL再写数据库. 
当写入失败，且恢复的时候，会发现WAL最后一条记录和数据库对应位置数据不一致，则需要根据WAL的内容进行回退，一直回退到trx_id信息处，
此间需要一直回收已经插入数据的存储空间.同时需要删除已经处理过的WAL记录. 删除完毕时候需要保证数据表存储空间位置指针与插入之前一致.