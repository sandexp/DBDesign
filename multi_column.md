#### 多列索引

按照B+树作为索引存储结构,根节点存储第一个属性的索引结构

根节点的叶子节点会指向第二个元素的索引结构，依次类推到第n个索引结构

对于数据库表更新的时候需要注意索引树的更新策略

假设插入一条记录包含第一第二个索引值X,Y
则需要做的步骤为
在第一个字段的索引树下插入X,获取插入成功的索引位置

根据索引位置找到第二个节点的索引树,将Y元素插入进去

这里插入包括插入与更新的逻辑

1. 如果所有元素插入都是已经存在的，且使用了唯一索引，则报错，如果不是就将存储位置信息加入到索引位置下