#### 简单子查询

1. IN/NOT IN
子查询作为一个临时数据表,拥有自己的表空间信息schema，数据块信息

其中IN/NOT IN 是一个谓词表达式,在扫描临时表的时候,查找到当前信息,返回true的谓词给上一级查询即可

2. 比较表达式> < >= <= =
这里后边的子查询必定是一个标量的子查询

首先检查子查询是否为标量子查询, 如果不是报错
如果是标量子查询, 使用标量子查询优化将其替换为其结果值给上一级查询即可

