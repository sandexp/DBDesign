#### 关于大文件TEXT的存储设计

TEXT文件在磁盘上进行连续存储, 其中的数据页的页号是连续的，在text数据头部需要加设首部信息 由于长度固定为4096字节
所以不需要进行长度指示, 只需要指示起始页号以及起始页内偏移量即可. 通过计算可以获得数据存储的结束位置.
反序列化的时候只需要根据schema获得这个头部信息(长度8字节),解析出这个起始页号和页内偏移量 即可需要加载到内存的数据范围.
通过实现序列化/反序列化函数就可以正确的读取text文件,同时为了支持null类型,text的信息也要注册到bitmap中,进行空值判断