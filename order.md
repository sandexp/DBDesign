#### 排序规则的实现
将需要选出来的字段组成临时表并进行页内的局部排序

当数据量比较大的时候,就需要进行磁盘文件直接的归并排序了，并将结果返回给客户端


###### 优化措施
选取主键的rowId信息配合关键字字段进行排序,这样相同的buffer下可以容纳更多的记录,归并排序的频次和数据量会小许多

这样只需要回表将对象的记录信息取出来拼接即可


此外需要底层数据类型支持排序函数逻辑,包括text类型